name: refresh-basecalc

on:
  schedule:
    - cron: "15 */6 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: refresh-basecalc
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    env:
      DJANGO_SETTINGS_MODULE: myproject.settings
      SECRET_KEY: github-actions-basecalc-refresh
      DEBUG: "False"
      SQLITE_DB_PATH: /tmp/basecalc-refresh.sqlite3
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Test basecalc
        run: python manage.py test --debug-mode basecalc
      - name: Migrate
        run: python manage.py migrate --noinput
      - name: Import existing basecalc history
        run: python manage.py import_basecalc_history --input basecalc/data/basecalc_history.json || true
      - name: Refresh basecalc
        run: python manage.py refresh_basecalc_data --export-history --export-path basecalc/data/basecalc_history.json
      - name: Finalize finance display data
        run: python manage.py finalize_finance_display_data
      - name: Output contract check
        run: python manage.py check_basecalc_output_contract --snapshot basecalc/data/latest_snapshot.json
      - name: Precompute explanation
        run: |
          python manage.py precompute_explanation \
            --output explanation/data/latest_snapshot.json \
            --history-output explanation/data/snapshot_history.json \
            --max-history-rows 500
      - name: Check explanation files exist
        run: |
          test -f explanation/data/latest_snapshot.json
          test -f explanation/data/snapshot_history.json
          test -f explanation/data/trade_outcomes.json
      - name: Evaluate explanation outcomes
        run: |
          python manage.py evaluate_explanation_outcomes \
            --snapshot-history explanation/data/snapshot_history.json \
            --input-outcomes explanation/data/trade_outcomes.json \
            --output explanation/data/trade_outcomes.json
      - name: Check explanation integrity
        run: python manage.py check_explanation_integrity
      - name: Data integrity check
        run: python manage.py check_basecalc_data_integrity
      - name: Commit updated history
        run: |
          if git diff --quiet -- basecalc/data/basecalc_history.json basecalc/data/basecalc_status.json basecalc/data/latest_snapshot.json explanation/data/latest_snapshot.json explanation/data/snapshot_history.json explanation/data/trade_outcomes.json static/finance_data_manifest.json; then
            echo "No basecalc data changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add basecalc/data/basecalc_history.json basecalc/data/basecalc_status.json basecalc/data/latest_snapshot.json explanation/data/latest_snapshot.json explanation/data/snapshot_history.json explanation/data/trade_outcomes.json static/finance_data_manifest.json
          git commit -m "Update basecalc data"
          git push
